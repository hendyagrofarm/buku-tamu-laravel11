@extends('layouts.admin')
@section('title','Divisi')
@section('page-title','Master Divisi')
@section('content')
<div class="mb-5 flex justify-end"><a href="{{ route('admin.divisions.create') }}" class="rounded-xl bg-emerald-700 px-4 py-3 text-sm font-bold text-white">+ Tambah Divisi</a></div>
<div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Nama</th><th class="px-5 py-3">Pegawai</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($divisions as $division)<tr><td class="px-5 py-4"><p class="font-bold">{{ $division->name }}</p><p class="max-w-lg text-xs text-slate-500">{{ $division->description ?: '-' }}</p></td><td class="px-5 py-4">{{ $division->employees_count }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $division->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $division->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td class="px-5 py-4"><div class="flex justify-end gap-2"><a href="{{ route('admin.divisions.edit',$division) }}" class="rounded-lg border px-3 py-2 text-xs font-bold">Edit</a><form method="POST" action="{{ route('admin.divisions.destroy',$division) }}" onsubmit="return confirm('Hapus divisi ini?')">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-xs font-bold text-red-600">Hapus</button></form></div></td></tr>@empty<tr><td colspan="4" class="px-5 py-10 text-center text-slate-500">Belum ada divisi.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $divisions->links() }}</div>
@endsection
