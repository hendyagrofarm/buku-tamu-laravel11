@extends('layouts.admin')
@section('title','Pegawai')
@section('page-title','Master Pegawai')
@section('content')
<div class="mb-5 flex items-center justify-between"><form class="flex gap-2"><input name="search" value="{{ request('search') }}" class="rounded-xl border border-slate-300 px-4 py-3 text-sm" placeholder="Cari pegawai">@if(auth()->user()->isAdmin())<select name="location_id" class="rounded-xl border border-slate-300 px-4 py-3 text-sm"><option value="">Semua lokasi</option>@foreach($locations as $loc)<option value="{{ $loc->id }}" @selected(request('location_id')==$loc->id)>{{ $loc->name }}</option>@endforeach</select>@endif<button class="rounded-xl bg-slate-900 px-4 py-3 text-sm font-bold text-white">Filter</button></form><a href="{{ route('admin.employees.create') }}" class="rounded-xl bg-emerald-700 px-4 py-3 text-sm font-bold text-white">+ Tambah Pegawai</a></div>
<div class="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Nama</th><th class="px-5 py-3">Lokasi</th><th class="px-5 py-3">Divisi</th><th class="px-5 py-3">Jabatan</th><th class="px-5 py-3">Status</th><th></th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($employees as $employee)<tr><td class="px-5 py-4 font-bold">{{ $employee->name }}<p class="text-xs text-slate-500">{{ $employee->email }}</p></td><td class="px-5 py-4">{{ $employee->location?->name ?? '-' }}</td><td class="px-5 py-4">{{ $employee->division->name }}</td><td class="px-5 py-4">{{ $employee->position }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $employee->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $employee->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td class="px-5 py-4 text-right"><a href="{{ route('admin.employees.edit',$employee) }}" class="font-bold text-blue-700">Edit</a></td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">Data tidak ditemukan.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $employees->links() }}</div>
@endsection
